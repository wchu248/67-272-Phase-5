<!-- Display a table showing information of all the pieces -->
<table class="bordered highlight centered responsive-table">
	<thead>
		<tr>
			<th>Name</th>
			<th>Price</th>
			<% if logged_in? && (current_user.role?(:admin) || current_user.role?(:manager)) %>
				<th>Inventory</th>
				<th></th>
			<% end %>
		</tr>
	</thead>

	<tbody>
		<% for item in @pieces %>
		<tr>
			<td><%= link_to item.name, item_path(item) %></td>
			<% if item.current_price.nil? %>
				<td>N/A</td>
			<% else %>
				<td><%= number_to_currency(item.current_price) %></td>
			<% end %>
			<% if logged_in? && (current_user.role?(:admin) || current_user.role?(:manager)) %>
				<td><%= number_with_delimiter(item.inventory_level) %></td>
				<td><a class="waves-effect waves-teal btn" <%= link_to "Edit", edit_item_path(item) %> </a></td>
			<% end %>		
		</tr>
		<% end %>
	</tbody>
</table>